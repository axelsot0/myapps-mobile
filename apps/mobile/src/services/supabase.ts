// Supabase client — placeholder until backend credentials are configured
// Full implementation will include auth, realtime sync, and edge function calls

// Dependencies: @supabase/supabase-js, expo-secure-store

// Configuration via environment variables (EAS secrets)
// EXPO_PUBLIC_SUPABASE_URL
// EXPO_PUBLIC_SUPABASE_ANON_KEY

// Placeholder — real implementation once Supabase project is provisioned
export const SUPABASE_URL = process.env['EXPO_PUBLIC_SUPABASE_URL'] ?? '';
export const SUPABASE_ANON_KEY = process.env['EXPO_PUBLIC_SUPABASE_ANON_KEY'] ?? '';
